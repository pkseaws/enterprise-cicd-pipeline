# enterprise-cicd-pipeline/.github/workflows/unified-build-deployment.yaml
name: Unified Build and Deploy

on:
  workflow_call:
    inputs:
      language:
        required: false
        type: string
        default: java
      env:
        required: false
        type: string
        default: dev
      upload_artifact:
        required: false
        type: boolean
        default: false
  

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        langugage: ${{ inputs.language }}
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Build Java
        if: ${{ inputs.language == 'java' }}
        uses: pkseaws/enterprise-cicd-pipeline/build-java@main

      - name: Build Python
        if: ${{ inputs.language == 'python' }}
        uses: ./build-python

      - name: Upload Artifact
        if: ${{ inputs.upload_artifact }}
        uses: ./upload-artifact
        with:
          environment: ${{ inputs.env }}
          language: ${{ inputs.language }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: ${{ inputs.env }}
      
    steps:
      - name: Deploy Java
        if: ${{ inputs.language == 'java' }}
        uses: pkseaws/enterprise-cicd-pipeline/deploy/deploy-java@main
        with:
          env: ${{ inputs.env }}
        #secrets:
         # deploy_token: ${{ secrets.JAVA_DEPLOY_SECRET }}

      - name: Deploy Python
        if: ${{ inputs.language == 'python' }}
        uses: ./deploy/deploy-python
        with:
          env: ${{ inputs.env }}
        #secrets:
         # deploy_token: ${{ secrets.PYTHON_DEPLOY_SECRET }}
