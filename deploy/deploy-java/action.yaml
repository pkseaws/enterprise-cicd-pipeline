name: Deploy Java
description: Deploy Java app
inputs:
  env:
    required: true
#secrets:
 # deploy_token:
  #  required: true
runs:
  using: "composite"
  steps:
    - run: echo "Deploying Java app to ${{ inputs.env }}"
      shell: bash
